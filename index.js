// calculating everything in millisecond
const second = 1000;
const minute = 60 * second;
const hour = 60 * minute;
const day = 24 * hour;


const timerFunction = () => {
    
    console.log(second);
    console.log(minute);
    console.log(hour);
    console.log(day);
};

timerFunction();